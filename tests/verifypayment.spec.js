import { test, expect } from '@playwright/test';

test('verify payment', async ({ page }) => {
    //launch url, 
    // login. 
    // select product, 
    // add to cart, checkout, 
    // fill details, 
    // place order, 
    // verify order

    make Payment

    make payment with bank
    make payment with card
    make payment with upi
    make payment with wallet
    